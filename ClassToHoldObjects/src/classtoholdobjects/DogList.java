package classtoholdobjects;

public class DogList {
    
    /* private Dog[] thelist = new Dog[5];
    private int i = 0;
    
    public void add(Dog d){
        if(i<thelist.length){ // check if arrey is full or not 
            thelist[i]=d; // if not it adds object to index
            System.out.println("Dog added at index "+i); // print out message 
            i++; // go to another counter
        }
    } */
    
}
